version: "2"

linters:
  default: standard
  enable:
    - staticcheck
  settings:
    errcheck:
      # Writes to stdout/stderr have no useful recovery in a CLI.
      exclude-functions:
        - fmt.Fprint
        - fmt.Fprintf
        - fmt.Fprintln
        - (*text/tabwriter.Writer).Flush
